#include "features/renderer/material.h"

using namespace wne;

void Material::rebuild()
{
}

void Material::bind(uint64 objectId, const Matrix4x4 &mMVP, const Matrix3x3 &mNormal, ModelDataType dataType)
{
}